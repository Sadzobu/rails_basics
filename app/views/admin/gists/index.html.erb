<div class="container">
  <h1><%= t('.header') %></h1>

  <table class="table">
    <tr>
      <th scope="col"><%= t('.question') %></th>
      <th scope="col"><%= t('.author') %></th>
      <th scope="col"><%= t('.url') %></th>
    </tr>

    <%= render @gists %>

  </table>
</div>
